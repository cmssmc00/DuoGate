# Optional local credentials. Copy to .env and fill in your own values.
OPENAI_API_KEY=
# For an OpenAI-compatible provider, also set its API endpoint:
# OPENAI_BASE_URL=https://your-provider.example/v1
